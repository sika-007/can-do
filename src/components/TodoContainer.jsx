import TodoBox from "./TodoBox";

const TodoContainer = () => {
  return (
    <div className="w-full h-screen bg-transparent flex flex-col items-center justify-center rounded-lg">
      <TodoBox />
    </div>
  );
};

export default TodoContainer;
